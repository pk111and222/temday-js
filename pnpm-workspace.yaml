packages:
  - .
  - docs

# VitePress and Rslib require esbuild's platform binary installation script.
# Keep the build-script allowlist minimal for reproducible CI installs.
allowBuilds:
  esbuild: true
